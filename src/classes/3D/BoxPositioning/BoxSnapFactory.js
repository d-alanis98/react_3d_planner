/**
 * @author Damián Alanís Ramírez
 * @version 1.0.0
 */

export default class BoxSnapFactory {
    constructor(movingObject, objects, initialPosition) {
        this.object = movingObject;
        this.objects = objects;
        this.initialPosition = initialPosition;
    }

    getNearestObject(){
        
    }
}